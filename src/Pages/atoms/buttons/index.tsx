import React from 'react';

import content from '../../../PageData/ButtonspageData.json';
import ComponentPage from '../../../Templates/ComponentPage';

const buttons = () => {
	return <ComponentPage content={content} />;
};

export default buttons;
